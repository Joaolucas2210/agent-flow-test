# Eval case: sum-bug

**Goal:** `solution.js` exports `sum(a, b)` that returns the numeric sum of two
numbers. It currently ships broken (a seeded bug). Make `test.js` pass.

**Done criteria (objective):** `node --test test.js` exits 0.

**Constraints:** touch only `solution.js`. Ponytail: least code that works.
